# المساهمة في HERP

شكرًا لرغبتك في تطوير HERP. يهدف المشروع إلى بناء نظام أعمال عربي يمكن لمجتمع المطورين فحصه وتوسيعه دون التضحية بعزل بيانات المؤسسات أو سلامة العمليات المحاسبية.

## سير العمل

| الخطوة | المطلوب |
|---|---|
| قبل البدء | افتح مسألة تصف الحاجة أو راجع مسألة قائمة. |
| الفرع | أنشئ فرعًا وصفيًا مثل `feature/inventory-transfers`. |
| التنفيذ | أبقِ كل إجراء بيانات محميًا ومقيدًا بـ `organizationId` ووحدة الصلاحية المناسبة. |
| التحقق | شغّل `pnpm check` و`pnpm test` قبل فتح طلب الدمج. |
| المراجعة | اشرح أثر التغيير على البيانات والصلاحيات ولقطات الواجهة عند تغيّر UI. |

## قواعد مهمة

لا تضف مفاتيح أو ملفات `.env` أو بيانات عملاء أو أسماء حقيقية للموظفين إلى الشفرة أو الاختبارات. لا تنشئ مسارًا يقرأ أو يعدّل سجلًا دون التحقق من المؤسسة التابعة له. يجب أن تظل قيود اليومية متوازنة، وأن تكون عمليات المخزون محمية من الرصيد السالب.

استخدم الإجراءات المعرّفة في `server/routers` والوصول إلى البيانات في `server/db.ts` أو المساعدات المخصصة. أضف اختبار Vitest عند تعديل منطق الأعمال، وحافظ على واجهة عربية قابلة للوصول وباتجاه RTL.

## طلبات الدمج

يجب أن يصف طلب الدمج المشكلة والحل والاختبارات المنفذة. عندما يغير الترحيل مخطط البيانات، أرفق ملف ترحيل Drizzle المولّد واذكر إن كان التغيير متوافقًا مع البيانات الموجودة.
