import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { trpc } from "@/lib/trpc";
import { Building2, CheckCircle2, ShieldCheck, Sparkles } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function SetupOrganization() {
  const [name, setName] = useState("");
  const [currency, setCurrency] = useState("SAR");
  const { user } = useAuth();
  const utils = trpc.useUtils();
  const bootstrap = trpc.herp.bootstrap.useMutation({ onSuccess: () => { toast.success("تم إنشاء مساحة عمل HERP بنجاح."); utils.herp.context.invalidate(); }, onError: (error) => toast.error(error.message) });
  return <main className="herp-login min-h-screen px-5 py-10" dir="rtl"><div className="grid w-full max-w-5xl overflow-hidden rounded-[28px] border border-white/[0.08] bg-[#0d1728] shadow-2xl lg:grid-cols-[1.1fr_0.9fr]"><section className="p-7 sm:p-12"><div className="flex size-12 items-center justify-center rounded-2xl bg-cyan-400 text-slate-950"><Building2 className="size-6" /></div><p className="mt-7 text-xs font-bold tracking-[0.18em] text-cyan-300">HERP · الإعداد الأولي</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-white sm:text-4xl">لنبدأ بتجهيز مساحة عمل مؤسستك</h1><p className="mt-4 max-w-xl leading-7 text-slate-400">سيتحول حسابك إلى مدير عام لهذه المؤسسة، ويمكنك بعد ذلك دعوة أعضاء الفريق ومنحهم صلاحيات دقيقة على الوحدات.</p><div className="mt-8 grid gap-5"><div className="grid gap-2"><Label htmlFor="org-name" className="font-semibold text-slate-200">اسم المؤسسة</Label><Input id="org-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="مثال: شركة الأفق" className="h-11 border-white/10 bg-white/[0.04] text-white placeholder:text-slate-600 focus-visible:ring-cyan-400" /></div><div className="grid gap-2"><Label htmlFor="currency" className="font-semibold text-slate-200">العملة الأساسية</Label><select id="currency" value={currency} onChange={(event) => setCurrency(event.target.value)} className="h-11 rounded-md border border-white/10 bg-[#142033] px-3 text-sm text-white outline-none focus:ring-2 focus:ring-cyan-400"><option value="SAR">SAR — ريال سعودي</option><option value="AED">AED — درهم إماراتي</option><option value="USD">USD — دولار أمريكي</option><option value="EGP">EGP — جنيه مصري</option></select></div><Button disabled={bootstrap.isPending || name.trim().length < 2} onClick={() => bootstrap.mutate({ name: name.trim(), currency })} className="mt-2 h-11 bg-cyan-400 text-slate-950 hover:bg-cyan-300">{bootstrap.isPending ? "جارٍ تجهيز المؤسسة…" : "إنشاء مساحة العمل"}</Button></div><p className="mt-5 text-xs text-slate-500">مرحبًا {user?.name || "بك"}. يمكنك تعديل بيانات المؤسسة لاحقًا من صفحة الإدارة.</p></section><aside className="relative overflow-hidden border-t border-white/[0.07] bg-gradient-to-b from-cyan-400/[0.09] via-[#0d1728] to-[#0d1728] p-7 sm:p-12 lg:border-r lg:border-t-0"><div className="absolute -top-16 -left-16 size-52 rounded-full bg-cyan-400/10 blur-3xl" /><h2 className="relative text-xl font-bold text-slate-100">ما الذي ستجده في HERP؟</h2><div className="relative mt-8 space-y-6">{[[ShieldCheck, "صلاحيات دقيقة", "أدوار للمدير العام ومدير الوحدة والموظف."], [Sparkles, "رؤية تنفيذية", "مؤشرات ولوحات تنبيه تعتمد على بياناتك الفعلية."], [CheckCircle2, "وحدات مترابطة", "موارد بشرية ومالية ومخزون وعلاقات عملاء وتقارير."]].map(([Icon, title, description]) => { const Glyph = Icon as typeof ShieldCheck; return <div key={String(title)} className="flex gap-3"><div className="flex size-9 shrink-0 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-300"><Glyph className="size-4" /></div><div><h3 className="font-semibold text-slate-200">{String(title)}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{String(description)}</p></div></div>; })}</div></aside></div></main>;
}
